node_modules/
dist/
.data/
.env
.env.local
# Wallet secrets must never be committed (spec Section 11).
.wallets/
*.keypair.json
coverage/
.next/
out/
*.tsbuildinfo
test-results/
playwright-report/
next-env.d.ts

# program binaries are committed gzipped; the harness inflates them
tests/fixtures/*.so

# Stage 3 program builds (the deploy keypair in target/deploy is a PRIVATE KEY)
programs/target/
