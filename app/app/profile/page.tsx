import { ProfileScreen } from "../../components/profile-screen";

/** Static route; everything is read client-side from the connected wallet. */
export default function ProfilePage() {
  return <ProfileScreen />;
}
