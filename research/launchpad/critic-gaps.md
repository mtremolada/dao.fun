## GAPS
[
 {
  "title": "Deployed Raydium CPMM binary: interface drift vs source revs, on-chain IDL, and current AmmConfig layout (creator-fee era)",
  "prompt": "You are researching for a team building a Solana Anchor (anchor-lang 0.30.1) bonding-curve launchpad that will CPI into the deployed mainnet Raydium CPMM program `CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C` (repo: github.com/raydium-io/raydium-cp-swap) to create the graduation pool. Their doctrine is 'never trust source repos over deployed binaries' (they were burned by a diverged spl-governance fork), and they plan to build the CPI against the `anchor-0.30.1` branch of github.com/raydium-io/raydium-cpi (package raydium-cpmm-cpi, last updated 2025-08-28) while the cp-swap master is now on anchor 0.32.1. Today is 2026-08-08. Answer with citations (URLs) for every claim:\n1. When was the mainnet CPMM program last upgraded (check solscan.io/account/CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C — program deploy/upgrade history), and does it publish an on-chain Anchor IDL (solscan 'Anchor Program IDL' tab or `anchor idl fetch`)? If an IDL is fetchable, capture the CURRENT `initialize` instruction: its 8-byte discriminator, exact ordered account list (names + writable/signer flags), and args.\n2. Diff the raydium-cp-swap source between the anchor-0.29/0.30-era code (pre-commit e1b98ac), commit e1b98ac (2025-05-14, anchor 0.31.0), and commit 609780f (2025-12-29, anchor 0.32.1): did the `initialize` instruction's args or account list change at any point, or only toolchain? Specifically look for creator-fee-era changes: `initialize_with_permission`, `creator_fee_on`, new accounts or args on plain `initialize`, and changes to swap instructions (`swap_base_input`/`swap_base_output`).\n3. The live config endpoint https://api-v3.raydium.io/main/cpmm-config shows config index 0 = D4FPEruKEHrG5TenZ2mpDGEfu1iUvTiqBxvpU8HLBvC2 with a `creatorFeeRate` field (500) — which means the deployed program has creator-fee logic. Establish the CURRENT `AmmConfig` account layout (field order, byte offsets, total size) from the newest source rev, and compare it to the `AmmConfig` struct in the raydium-cpmm-cpi `anchor-0.30.1` branch. Does the 0.30.1 CPI crate's struct/account-list still deserialize/match the deployed layout, or is it stale (missing creatorFeeRate etc.)? If stale, what exactly diverges?\n4. Enumerate ALL current mainnet AmmConfigs from api-v3.raydium.io/main/cpmm-config: address, index, tradeFeeRate, createPoolFee, and whether any has `disable_create_pool` set. Confirm plain `initialize` is still permissionless on config index 0 today.\n5. Confirm the mainnet `create_pool_fee` receiver `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8` is still the hardcoded fee receiver in the deployed program's era (check the source rev constant `create_pool_fee_reveiver::ID` at each rev — did it ever change?).\nOutput raw facts: dates, discriminator bytes, ordered account lists, field layouts with offsets, addresses, and per-item source URLs. Flag anything you could not verify."
 },
 {
  "title": "Devnet graduation story: devnet CPMM AmmConfig addresses/fees, fee receiver, program parity, and CPI-crate devnet feature constants",
  "prompt": "You are researching the DEVNET story for a Solana launchpad that graduates tokens into Raydium CPMM pools. The team will run end-to-end devnet validation (their gate process requires real devnet transactions) before mainnet. Known so far: devnet CPMM program `DRaycpLY18LhpbydsBWbVJtxpNv9oXPgjRSfpF2bWpYb`, devnet create-pool-fee receiver `3oE58BKVt8KuYkGxx8zBojugnymWmBiyafWgMrnb6eYy` (from raydium-cp-swap lib.rs), mainnet createPoolFee = 150,000,000 lamports. Today is 2026-08-08. Find, with citation URLs for every claim:\n1. The concrete DEVNET AmmConfig account addresses for the CPMM program (index, address, tradeFeeRate, createPoolFee lamports, disable_create_pool flag). Sources to try: the raydium-io/raydium-cpi-example repo README (it lists devnet AmmConfig + fee-receiver test accounts for the cpmm-cpi example), docs.raydium.io devnet/developer pages, any api-v3 devnet endpoint (e.g. api-v3-devnet.raydium.io or a devnet query param), and Raydium's devnet UI. Report the actual addresses and fee values, not just where to find them.\n2. Is the devnet CPMM deployment kept in version parity with mainnet (compare last-upgrade dates on solscan/explorer for devnet `DRaycpLY18…` vs mainnet `CPMMoo8L…`)? If devnet lags or leads, which interface does it expose (does devnet have the creator-fee-era `initialize` / AmmConfig layout)?\n3. What exactly does the `devnet` cargo feature flag change in raydium-cp-swap / raydium-cpmm-cpi / raydium-cpi-example (which constants switch: program ID, amm_config admin, create_pool_fee receiver)? Quote the cfg blocks.\n4. Practicalities of creating a CPMM pool on devnet in 2025-2026: is permissionless `initialize` known to work on devnet today (find any recent tutorial, issue, or devnet tx example); what does one pool creation cost on devnet all-in (createPoolFee + rent), and does that fit within standard devnet airdrop limits (what are current faucet limits per request)? Any known breakage reports (GitHub issues on raydium-cp-swap / raydium-cpi-example about devnet configs being closed, changed, or unfunded)?\n5. Also confirm devnet program IDs for anything else the graduation path touches: Metaplex Token Metadata (`metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s` — same on devnet?), and the devnet LP-lock (Burn & Earn) program `DLockwT7X7sxtLmGH9g5kmfcjaBtncdbUmi738m5bvQC` — is it live and in parity with mainnet `LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE`?\nReturn raw addresses, lamport values, dates, and URLs. Explicitly flag any item you could not verify so the team knows to verify it on-chain themselves."
 },
 {
  "title": "All-in migration cost (rent + fee, exact lamports), wSOL/mint-ordering mechanics, and the exact Burn & Earn LP-lock instruction interface",
  "prompt": "You are researching two load-bearing details for a Solana Anchor program that, on bonding-curve completion, CPIs into Raydium CPMM (`CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C`) `initialize` to create the graduation pool, then either burns the LP or locks it via Raydium's Burn & Earn LP-lock program (`LockrWmn6K5twhz3y9w1dQERbmgSaRkfnTeTKbpofwE`, closed source) so a DAO treasury can hold the Fee Key NFT and claim pool fees forever. The bonding curve must reserve enough SOL to pay migration, so exact numbers matter. Today is 2026-08-08. With citation URLs for every claim:\n1. Compute the ALL-IN lamports the `initialize` payer spends: the 150,000,000-lamport createPoolFee PLUS rent-exemption for every account initialized in the instruction. Get exact account sizes from raydium-cp-swap source (github.com/raydium-io/raydium-cp-swap): PoolState LEN, ObservationState LEN (how many observation slots × bytes), lp_mint (SPL mint, 82 bytes), token_0_vault and token_1_vault (165 bytes each), creator_lp_token ATA (165 bytes). Convert sizes to rent-exempt lamports (use the standard rent formula: (128 + size) × 6960 lamports/byte-year × 2 years). Cross-check the total against a REAL recent mainnet CPMM pool-creation transaction on solscan/solana.fm (find one via the create_pool_fee receiver `DNXgeM9EiiaAbaWvwjHj9fQQLAX5ZsfHyvmYUNRAdNC8`'s recent activity) — report the actual SOL balance change of the payer in that tx.\n2. Confirm the fee-payment mechanics: create_pool_fee receiver is a wSOL ATA and the program transfers lamports then calls sync_native — quote the source lines. Confirm the SOL side of the pool must be provided as wrapped SOL in `creator_token_0`/`creator_token_1`, and state the exact requirement: token_0_mint must sort byte-wise below token_1_mint, WSOL mint = `So11111111111111111111111111111111111111112` — so a migrating program must branch on whether the launched mint sorts above or below WSOL. Confirm from source whether pool_state may be a non-canonical keypair/PDA signer (the `proxy_initialize_random_pool` path in raydium-io/raydium-cpi-example) and that a CPI caller can sign it with invoke_signed.\n3. Extract the EXACT LP-lock interface, since the lock program has no public source or IDL in raydium-io/raydium-idl. Sources: raydium-io/raydium-sdk-V2 (search src for lockLiquidity / lock instruction builders for CPMM), raydium-io/raydium-sdk-V2-demo (src/cpmm/lockLiquidity.ts, harvestLockLiquidity.ts), and the raydium_launchpad IDL (raw.githubusercontent.com/raydium-io/raydium-idl/master/raydium_launchpad/raydium_launchpad.json — its migrate_to_cpswap account list names lock_program / lock_authority / lock_lp_vault). Report: the lock instruction's name and 8-byte discriminator, full ordered account list with writable/signer flags, PDA seeds (e.g. 'lock_cp_authority_seed', locked-liquidity PDA, fee-NFT mint derivation), args, and the harvest/collect-fee instruction's same details. Is locking permissionless (any LP holder can lock)? Can the Fee Key NFT be minted to / held by an arbitrary address including a program PDA (i.e. a DAO treasury), and is it a regular transferable Metaplex NFT?\n4. Note anything about lock-program versioning: has the Burn & Earn program been upgraded in 2025-2026, and does the SDK still target the same instruction layout today?\nReturn raw numbers (byte sizes, lamport totals), ordered account lists, discriminators, seeds, and per-item URLs. Flag anything unverifiable from public sources so the team measures it against the dumped binary instead."
 }
]
